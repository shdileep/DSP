export const qaDatabase = [
  {
    id: 'location',
    keywords: ['location', 'based', 'live', 'address', 'reside', 'delhi', 'chennai', 'guntur', 'india', 'place', 'city', 'telangana', 'hyderabad'],
    answer: "Dileep is currently based in Hyderabad, Telangana. Reach him at dileepgalla200056@gmail.com."
  },
  {
    id: 'current_role',
    keywords: ['current role', 'working as', 'current job', 'now working', 'present role', 'working at'],
    answer: "He is currently working as an AI/ML Architect & Full Stack Engineer Intern at Easehawk Technologies Pvt. Ltd. (May 2026 – Present)."
  },
  {
    id: 'internships',
    keywords: ['internship', 'internships', 'intern', 'experience', 'worked at'],
    answer: "Dileep completed AI/ML internships at Easehawk Technologies and Externsclub, and a Web Development internship at Engineer Core."
  },
  {
    id: 'education',
    keywords: ['study', 'college', 'vit', 'vellore', 'university', 'software engineering', 'degree', 'education'],
    answer: "Dileep holds an Integrated M.Tech in Software Engineering from Vellore Institute of Technology (VIT), Chennai (Aug 2021 – May 2026)."
  },
  {
    id: 'graduation',
    keywords: ['graduated', 'graduate', 'did he graduate', 'has he graduated', 'graduation', 'is he graduated'],
    answer: "Yes, Dileep recently graduated in May 2026 with an Integrated M.Tech in Software Engineering from Vellore Institute of Technology (VIT), Chennai."
  },
  {
    id: 'patent',
    keywords: ['patent', 'patents', 'utility patent', '202641010900', 'invented', 'garbage', 'optimizer'],
    answer: "Dileep filed a utility patent for a Waste Operations Optimizer [Application Number: 202641010900]."
  },
  {
    id: 'scrum',
    keywords: ['scrum', 'agile', 'infosys', 'infosys springboard', 'scrum master'],
    answer: "Dileep holds an 'Agile Scrum in Practice' certification issued by Infosys Springboard."
  },
  {
    id: 'hackerrank',
    keywords: ['hackerrank', 'react certificate', 'sql certificate', 'software engineer intern'],
    answer: "Dileep holds React, SQL, and Software Engineer Intern certifications from HackerRank."
  },
  {
    id: 'freecodecamp',
    keywords: ['freecodecamp', 'responsive web design', 'html', 'css', 'fcc'],
    answer: "Dileep holds a 'Legacy Responsive Web Design V8' certification from freeCodeCamp."
  },
  {
    id: 'ibm',
    keywords: ['ibm', 'coursera', 'data analytics', 'genai powered'],
    answer: "Dileep completed the 'GenAI Powered Data Analytics' certification from IBM / Coursera."
  },
  {
    id: 'nexttrip',
    keywords: ['nexttrip', 'ticket', 'bus', 'booking', 'moxsend'],
    answer: "NEXTTRIP is a Full-Stack AI Bus Ticket Booking Engine in Express.js and PostgreSQL. It solves rigid pricing and concurrency bottlenecks."
  },
  {
    id: 'ujjwal',
    keywords: ['ujjwal', 'waste', 'garbage route', 'route optimizer', 'mapbox', 'dijkstra'],
    answer: "UJJWAL-HUB is an Intelligent Waste Collection & Route Optimization platform using K-Means and Dijkstra/A* routing heuristics."
  },
  {
    id: 'fitmitra',
    keywords: ['fitmitra', 'fitness', 'exercise', 'pose class', 'tflite', 'kotlin'],
    answer: "FITMITRA is an ML-Based Android Fitness App with a TFLite pose classifier and PyTorch BMR estimation."
  },
  {
    id: 'skills',
    keywords: ['languages', 'tech stack', 'skills', 'technologies', 'programming', 'docker', 'python', 'react', 'fastapi'],
    answer: "Dileep specializes in Python, React, FastAPI, Docker, PyTorch, LangChain, PostgreSQL, and Agentic AI workflows."
  },
  {
    id: 'nodemailer',
    keywords: ['nodemailer', 'mail server', 'email sender', 'smtp'],
    answer: "He has experience building mock email servers using Nodemailer and Python to test mail routing protocols."
  },
  {
    id: 'zustand',
    keywords: ['zustand', 'state management', 'render cycles'],
    answer: "Yes, he engineered React frontend components with Zustand state management, reducing render cycles to under 80ms."
  },
  {
    id: 't5',
    keywords: ['t5', 'resumeai', 'hallucinations', 'transformer'],
    answer: "He built 'ResumeAI' using a fine-tuned T5 transformer and RAG, cutting hallucinations by 25%."
  },
  {
    id: 'random_forest',
    keywords: ['random forest', 'classifier', 'machine learning', 'ml model'],
    answer: "Dileep trained Random Forest classifiers with custom TF-IDF extractions, achieving 91.4% target accuracy."
  },
  {
    id: 'aws',
    keywords: ['aws', 'ecs', 'cloud', 'deployment', 'clusters', 'uptime'],
    answer: "He deployed 15+ Dockerized microservices on AWS ECS, ensuring 99.9% uptime during traffic surges."
  },
  {
    id: 'node',
    keywords: ['node.js', 'node', 'express', 'express.js', 'backend'],
    answer: "Dileep uses Node.js and Express for low-latency backend APIs, reservation systems, and routing solvers."
  },
  {
    id: 'kotlin',
    keywords: ['kotlin', 'android', 'mobile app', 'pose class', 'biometric'],
    answer: "He built 'FitMitra', an Android fitness app in Kotlin with a TensorFlow Lite pose classifier (82% accuracy)."
  },
  {
    id: 'pytorch',
    keywords: ['pytorch', 'bmr', 'calorie', 'macros', 'splits'],
    answer: "Dileep engineered PyTorch-based BMR and calorie estimation heuristics, generating macro splits under 150ms."
  },
  {
    id: 'vercel',
    keywords: ['vercel', 'firebase', 'hosting'],
    answer: "He uses Vercel for hosting React/Vite frontends, and Firebase for serverless deployments and authentication."
  },
  {
    id: 'git',
    keywords: ['git', 'github', 'gitlab', 'ci/cd', 'pipelines', 'version control'],
    answer: "Dileep utilizes Git/GitHub/GitLab for version control and automates builds with AWS ECS CI/CD pipelines."
  },
  {
    id: 'locks',
    keywords: ['locks', 'row-level', 'collision', 'postgres locks'],
    answer: "Dileep secured transactions using PostgreSQL row-level locks, reducing database contention under heavy queues by 35%."
  },
  {
    id: 'celery',
    keywords: ['celery', 'workers', 'multi-agent workspace'],
    answer: "Dileep orchestrated multi-agent workspace pipelines using FastAPI and Celery, accelerating request throughput by 42%."
  },
  {
    id: 'contact',
    keywords: ['contact', 'email', 'linkedin', 'github', 'phone', 'reach', 'write to'],
    answer: "Dileep is currently based in Hyderabad, Telangana. Reach him at dileepgalla200056@gmail.com."
  },
  {
    id: 'hackerearth',
    keywords: ['hackerearth', 'hackathon', 'hackathons', 'competition', 'winner', 'algorithms'],
    answer: "He is a state hackathon enthusiast and has solved complex algorithmic problems on HackerEarth."
  }
];

export function lookupQaDatabase(query) {
  const lowerQuery = query.toLowerCase();

  // Specific checks for high-priority technical terms
  if (lowerQuery.includes('graduated') || lowerQuery.includes('graduate') || lowerQuery.includes('graduation') || lowerQuery.includes('did he graduate') || lowerQuery.includes('is he graduated')) {
    return qaDatabase.find(e => e.id === 'graduation').answer;
  }
  if (lowerQuery.includes('zustand') || lowerQuery.includes('render cycles')) {
    return qaDatabase.find(e => e.id === 'zustand').answer;
  }
  if (lowerQuery.includes('t5') || lowerQuery.includes('resumeai')) {
    return qaDatabase.find(e => e.id === 't5').answer;
  }
  if (lowerQuery.includes('random forest') || lowerQuery.includes('classifier')) {
    return qaDatabase.find(e => e.id === 'random_forest').answer;
  }
  if (lowerQuery.includes('aws') || lowerQuery.includes('ecs') || lowerQuery.includes('cloud')) {
    return qaDatabase.find(e => e.id === 'aws').answer;
  }
  if (lowerQuery.includes('node') || lowerQuery.includes('express')) {
    return qaDatabase.find(e => e.id === 'node').answer;
  }
  if (lowerQuery.includes('kotlin') || lowerQuery.includes('android')) {
    return qaDatabase.find(e => e.id === 'kotlin').answer;
  }
  if (lowerQuery.includes('pytorch') || lowerQuery.includes('bmr') || lowerQuery.includes('calorie')) {
    return qaDatabase.find(e => e.id === 'pytorch').answer;
  }
  if (lowerQuery.includes('vercel') || lowerQuery.includes('firebase')) {
    return qaDatabase.find(e => e.id === 'vercel').answer;
  }
  if (lowerQuery.includes('git') || lowerQuery.includes('ci/cd') || lowerQuery.includes('pipeline')) {
    return qaDatabase.find(e => e.id === 'git').answer;
  }
  if (lowerQuery.includes('locks') || lowerQuery.includes('row-level') || lowerQuery.includes('collision')) {
    return qaDatabase.find(e => e.id === 'locks').answer;
  }
  if (lowerQuery.includes('celery') || lowerQuery.includes('workers') || lowerQuery.includes('multi-agent')) {
    return qaDatabase.find(e => e.id === 'celery').answer;
  }
  if (lowerQuery.includes('playwright') || lowerQuery.includes('e2e') || lowerQuery.includes('applitool') || lowerQuery.includes('testing')) {
    return qaDatabase.find(e => e.id === 'playwright').answer;
  }
  if (lowerQuery.includes('nodemailer') || lowerQuery.includes('mail server') || lowerQuery.includes('email sender') || lowerQuery.includes('smtp')) {
    return qaDatabase.find(e => e.id === 'nodemailer').answer;
  }
  if (lowerQuery.includes('patent') || lowerQuery.includes('202641010900') || lowerQuery.includes('optimizer') || lowerQuery.includes('garbage')) {
    return qaDatabase.find(e => e.id === 'patent').answer;
  }
  if (lowerQuery.includes('hackerearth') || lowerQuery.includes('hackathon')) {
    return qaDatabase.find(e => e.id === 'hackerearth').answer;
  }
  if (lowerQuery.includes('scrum') || lowerQuery.includes('agile')) {
    return qaDatabase.find(e => e.id === 'scrum').answer;
  }
  if (lowerQuery.includes('hackerrank')) {
    return qaDatabase.find(e => e.id === 'hackerrank').answer;
  }
  if (lowerQuery.includes('freecodecamp') || lowerQuery.includes('fcc') || lowerQuery.includes('responsive')) {
    return qaDatabase.find(e => e.id === 'freecodecamp').answer;
  }
  if (lowerQuery.includes('ibm') || lowerQuery.includes('coursera') || lowerQuery.includes('data analytics')) {
    return qaDatabase.find(e => e.id === 'ibm').answer;
  }
  if (lowerQuery.includes('location') || lowerQuery.includes('based') || lowerQuery.includes('live') || lowerQuery.includes('hyderabad') || lowerQuery.includes('telangana')) {
    return qaDatabase.find(e => e.id === 'location').answer;
  }
  if (lowerQuery.includes('ujjwal') || lowerQuery.includes('waste') || lowerQuery.includes('mapbox') || lowerQuery.includes('dijkstra')) {
    return qaDatabase.find(e => e.id === 'ujjwal').answer;
  }
  if (lowerQuery.includes('nexttrip') || lowerQuery.includes('booking') || lowerQuery.includes('bus') || lowerQuery.includes('ticket')) {
    return qaDatabase.find(e => e.id === 'nexttrip').answer;
  }
  if (lowerQuery.includes('fitmitra') || lowerQuery.includes('fitness') || lowerQuery.includes('pose') || lowerQuery.includes('tflite')) {
    return qaDatabase.find(e => e.id === 'fitmitra').answer;
  }
  if (lowerQuery.includes('contact') || lowerQuery.includes('email') || lowerQuery.includes('reach') || lowerQuery.includes('linkedin') || lowerQuery.includes('github') || lowerQuery.includes('portfolio') || lowerQuery.includes('phone') || lowerQuery.includes('hire')) {
    return qaDatabase.find(e => e.id === 'contact').answer;
  }
  if (lowerQuery.includes('study') || lowerQuery.includes('college') || lowerQuery.includes('vit') || lowerQuery.includes('vellore') || lowerQuery.includes('software engineering') || lowerQuery.includes('degree') || lowerQuery.includes('education')) {
    return qaDatabase.find(e => e.id === 'education').answer;
  }

  return null;
}
